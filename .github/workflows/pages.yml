name: Deploy landing site to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "landing/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# One deployment at a time. Queued runs wait rather than cancel, so a push is
# never dropped.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: landing
    env:
      NEXT_PUBLIC_SITE_ORIGIN: https://tashukbiz.github.io
      NEXT_PUBLIC_BASE_PATH: /nativesheets
      NEXT_PUBLIC_GA_MEASUREMENT_ID: ${{ vars.NEXT_PUBLIC_GA_MEASUREMENT_ID }}
      NEXT_PUBLIC_AD_STATE: ${{ vars.NEXT_PUBLIC_AD_STATE || 'pending-review' }}
      NEXT_PUBLIC_ADSENSE_CLIENT: ${{ vars.NEXT_PUBLIC_ADSENSE_CLIENT || 'ca-pub-7074622316308983' }}
      NEXT_PUBLIC_ADSENSE_SLOT_ARTICLE: ${{ vars.NEXT_PUBLIC_ADSENSE_SLOT_ARTICLE || '2864779511' }}
      NEXT_PUBLIC_ADSENSE_CMP_READY: ${{ vars.NEXT_PUBLIC_ADSENSE_CMP_READY || 'false' }}
    steps:
      - uses: actions/checkout@v4

      # No version here on purpose: package.json's packageManager field pins
      # pnpm, and setting both makes this action fail with a version conflict.
      # The path is explicit because the action reads the repository root by
      # default, and defaults.run.working-directory does not apply to actions.
      - uses: pnpm/action-setup@v4
        with:
          package_json_file: landing/package.json

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: landing/pnpm-lock.yaml

      - run: pnpm install --frozen-lockfile

      - run: pnpm run typecheck

      - run: pnpm run lint

      - run: pnpm run test

      - run: pnpm run build

      - run: pnpm run verify

      - uses: actions/upload-pages-artifact@v3
        with:
          path: landing/out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
